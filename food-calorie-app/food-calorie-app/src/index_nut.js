// index.js

import React from 'react';
import ReactDOM from 'react-dom';
import nut from './nut';

ReactDOM.render(
  <React.StrictMode>
    <nut />
  </React.StrictMode>,
  document.getElementById('root')
);
